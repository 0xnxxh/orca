// Moved to src/shared so the renderer's reattach re-arm scans with the same
// implementation the daemon mirrors from (see terminal-mouse-mode-sequences).
export { TerminalMouseModeMirror } from '../../shared/terminal-mouse-mode-sequences'
