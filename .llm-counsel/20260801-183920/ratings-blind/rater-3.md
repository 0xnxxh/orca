# Blind peer ratings — rater-3

| report | evidence | regression_catch | false_positive_risk | actionability | best_unique | overreach |
|---|---|---|---|---|---|---|
| A | 5 | 3 | 5 | 5 | Cap-evicted (not-timed-out) straggler escapes BOTH `answeredSince` yield and `noteFailure` suppression — only report to see that `timedOut`-armed guards leave the eviction path unguarded, with the exact ownership-token fix. | Asserts the 30s timeout error is "correctly not negative-cached" and treats that as strictly good — it never follows `return null` upward, where it becomes a definitive cached "no review" (B/F3, C/#5). A miss dressed as a cleared claim. |
| B | 5 | 5 | 4 | 5 | `FORGE_PROVIDERS[0]` is GitLab, so the *first* `remote get-url` on the path is still unbounded (F1) + permanent settle-only coalescing below the deadline (F2) means post-deadline retries re-join a dead promise — this pair falsifies the branch's central "recover in-session" claim. | F9 (WSL kill leaves distro-side git orphaned) is directly contradicted by the runner's `killSpawnedCommandTree` wsl.exe/taskkill handling; F10's ms-collision is unreachable in practice. Also F1/F2/F4 are pre-existing, not branch-introduced, yet carry High. |
| C | 4 | 4 | 3 | 4 | Timed-out straggler storing a late `null` over a still-cached *open* review while only a pre-refresh entry exists (`fetchedAt < startedAt`) — plus a concrete t0…t3 timeline and 4 named test gaps. | #1/#2 at High is inflated: the straggler's answer is genuinely newer than the entry it replaces (late adoption is the design), and replacement B still stores its own newer answer on completion, so "never joins B" bounds at B's duration, not 15 minutes. |
| D | 4 | 3 | 5 | 3 | Reverse-order completion of two *timed-out* attempts: A stores after B started, so `fetchedAt >= startedAt` discards B's newer answer — an ordering inversion no other report identifies. | none (severities are calibrated; scope is just narrow) |

## Notes
- Sharpest split: A calls the timeout-error swallow correct, B/C call it a definitive cached "no review" that permits duplicate PR creation. B's chain (`getOwnerRepoForRemote` → null → no provider → cached `{review:null}` → `not_found`) is traced end-to-end and looks right; A's clearance covers only the 5-minute identity negative cache, a different layer.
- B is the only report to leave the diff and audit what the deadline actually wraps; that is where the load-bearing findings are, and its two measured harness numbers (36 calls/2h, 31 leaked/8h) are the only quantified claims in the set.
- A is the most trustworthy per-claim and the best at falsification bookkeeping, but its Low/Info framing of detached-lookup accumulation understates what three other raters independently flagged as the headline load regression.
- All four converge on unbounded detached lookups; C and D add distinct, non-overlapping ordering races (C: pre-refresh entry; D: two timed-out attempts). Both are worth a test each.
- D is correct but thin — three findings, no fix shapes, and it re-derives the cap-eviction issue without reaching A's overwrite/`noteFailure` consequences.
